const Loader = () => {
    return (
        <div className="container_loader">
            <div className="code-loader">
                <span>{"{"}</span>
                <span>{"}"}</span>
            </div>
        </div>
    );
};

export default Loader;
